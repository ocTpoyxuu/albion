Reciv game

-- 1 Компиляция --
-- 1.1 Версия для отладки --
Чтобы скомпилировать версию для отладки, выполните сценарий ./Debug. Если всё пройдёт успешно, то программа будет здесь: ./bin/appDBG

-- 1.2 Релизная версия --
Чтобы скомпилировать релизную версию, выполните сценарий ./Release. Если всё пройдёт успешно, то программа будет здесь: ./bin/app

